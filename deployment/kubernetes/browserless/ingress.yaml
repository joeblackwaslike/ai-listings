apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: browserless-ts
  namespace: ai-listings
  annotations:
    tailscale.com/proxy-group: cluster-ingress
    tailscale.com/hostname: browserless
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: browserless
      port:
        number: 3000
  tls:
    - hosts:
        - browserless
