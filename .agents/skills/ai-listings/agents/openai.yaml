interface:
  display_name: "Ai Listings"
  short_description: "Repo-specific patterns and workflows for ai-listings"
  default_prompt: "Use the ai-listings repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true